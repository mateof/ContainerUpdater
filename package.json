{
  "name": "container-updater",
  "version": "0.22.0",
  "private": true,
  "description": "Panel autoalojado para vigilar y actualizar las imagenes Docker de un NAS Synology",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @cu/shared && npm run build -w @cu/server && npm run build -w @cu/web",
    "dev": "npm run dev -w @cu/server & npm run dev -w @cu/web",
    "dev:server": "npm run dev -w @cu/server",
    "dev:web": "npm run dev -w @cu/web",
    "test": "npm run test --workspaces --if-present",
    "test:watch": "npm run test:watch -w @cu/server",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run typecheck",
    "version:patch": "npm version patch --no-git-tag-version --no-workspaces-update",
    "version:minor": "npm version minor --no-git-tag-version --no-workspaces-update",
    "version:major": "npm version major --no-git-tag-version --no-workspaces-update",
    "docker:build": "docker build --build-arg APP_VERSION=$(node -p \"require('./package.json').version\") -t container-updater:local .",
    "docker:push": "docker buildx build --platform linux/amd64,linux/arm64 --build-arg APP_VERSION=$(node -p \"require('./package.json').version\") -t ghcr.io/mateof/container-updater:$(node -p \"require('./package.json').version\") --push ."
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
